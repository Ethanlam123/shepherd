{
  "name": "shepherd",
  "private": true,
  "scripts": {
    "build:ui": "esbuild ui/panel.ts --bundle --format=esm --outfile=ui/dist/panel.js",
    "check": "tsc --noEmit",
    "dev": "tauri dev",
    "build": "tauri build",
    "sidecar": "./scripts/sidecar.sh",
    "release": "./scripts/release.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.25",
    "typescript": "^5"
  }
}
